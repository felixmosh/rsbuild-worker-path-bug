{
  "name": "rsbuild-worker-bug",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "rsbuild dev --open",
    "build": "rsbuild build",
    "preview": "rsbuild preview"
  },
  "devDependencies": {
    "@rsbuild/core": "1.0.1-beta.11",
    "@types/node": "^22.2.0",
    "typescript": "^5.5.2"
  }
}
